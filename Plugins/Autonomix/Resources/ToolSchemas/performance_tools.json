{
  "schema_version": "2.0.0",
  "domain": "performance_tools",
  "min_plugin_version": "1.0.0",
  "tools": [
    {
      "name": "get_performance_stats",
      "description": "Get current performance statistics: FPS and frame time from the editor's active delta. Use this as the starting diagnostic. If FPS is low, use run_stat_command with 'unit' to split into Game/Draw/GPU time to identify the bottleneck tier.",
      "input_schema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "get_memory_stats",
      "description": "Get current platform memory usage: used physical, available physical, virtual memory, and peak usage. Use this to determine if the project is hitting memory pressure. If physical memory is near system total, consider texture streaming, virtual texturing, or asset LOD optimization.",
      "input_schema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "run_stat_command",
      "description": "Toggle a stat overlay in the editor viewport. Key stat commands:\n- 'unit': Frame/Game/Draw/GPU breakdown — use this FIRST to identify bottleneck\n- 'fps': Frames per second\n- 'gpu': Per-pass GPU timings (BasePass, Shadow, Translucency, etc.)\n- 'scenerendering': Draw call counts\n- 'memory': Texture/render target memory pools\n- 'drawcount': Primitive draw call volume\n- 'streaming': Texture streaming diagnostics\nNOTE: stat overlays are displayed in the viewport. To capture data programmatically, use start_csv_profiler instead.",
      "input_schema": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "The stat sub-command, e.g. 'unit', 'fps', 'gpu', 'scenerendering', 'memory', 'drawcount', 'streaming'"
          }
        },
        "required": ["command"]
      }
    },
    {
      "name": "analyze_asset_sizes",
      "description": "Analyze disk sizes of all assets in the project (or a filtered sub-path) and return the top N largest assets. Use this to identify oversized textures, meshes, or audio files that are inflating package size. A 4K texture (4096x4096) takes ~85MB uncompressed — look for unexpected large assets and use configure_static_mesh or set_renderer_setting to enforce LOD/texture limits.",
      "input_schema": {
        "type": "object",
        "properties": {
          "path_filter": {
            "type": "string",
            "description": "Optional package path prefix to limit the search, e.g. '/Game/Characters'. Leave empty to scan all assets."
          },
          "top_n": {
            "type": "integer",
            "description": "Number of largest assets to return (default: 20)"
          }
        },
        "required": []
      }
    },
    {
      "name": "get_cvar",
      "description": "Read the current value and help text of a single Unreal Engine Console Variable (CVar). Use this before set_cvar to confirm current state. Common optimization CVars:\n- r.AntiAliasingMethod (0=none,1=FXAA,2=TAA,3=MSAA,4=TSR)\n- r.BloomQuality (0=off, 5=best)\n- r.AmbientOcclusionLevels (0=off, 1-3)\n- r.ForwardShading (0=deferred, 1=forward)\n- r.TemporalAA.Quality (0=off)\n- r.FullScreenMode (0=exclusive, 1=windowed)\n- a.URO.Enable (1=enable animation Update Rate Optimization)",
      "input_schema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Full CVar name, e.g. 'r.BloomQuality' or 'r.AmbientOcclusionLevels'"
          }
        },
        "required": ["name"]
      }
    },
    {
      "name": "set_cvar",
      "description": "Set a Console Variable (CVar) to a new value at runtime. This is TRANSIENT — it applies immediately but is lost on editor restart. Use this for profiling experiments and A/B testing. To make a change permanent, follow up with write_config_value or set_renderer_setting.\n\nKey optimization CVars (value ranges):\n| CVar | Values | Effect |\n|------|--------|--------|\n| r.AntiAliasingMethod | 0-4 | 0=none,1=FXAA,2=TAA,3=MSAA,4=TSR |\n| r.BloomQuality | 0-5 | 0=off (fastest), 5=best (slowest) |\n| r.AmbientOcclusionLevels | 0-3 | 0=off, saves GPU |\n| r.AmbientOcclusion.FadeRadiusScale | 0.01-1.0 | SSAO fade radius |\n| r.ForwardShading | 0-1 | 1=forward (cheaper constant cost) |\n| r.FullScreenMode | 0-1 | 0=exclusive (faster) |\n| a.URO.Enable | 0-1 | 1=throttle distant anim (saves CPU) |\n| r.VolumetricCloud | 0-1 | 0=disable expensive volumetric clouds |\n| r.FreeSkeletalMeshBuffers | 0-1 | 1=free CPU-side mesh buffers |\n| r.ForceStripAdjacencyDataDuringCooking | 0-1 | 1=strip tessellation data |\n\nWorkflow: set_cvar (test) → measure via get_performance_stats → if improvement confirmed → write_config_value or set_renderer_setting to persist.",
      "input_schema": {
        "type": "object",
        "properties": {
          "name": {
            "type": "string",
            "description": "Full CVar name, e.g. 'r.BloomQuality'"
          },
          "value": {
            "type": "string",
            "description": "New value as string, e.g. '0', '1', '75.0'"
          }
        },
        "required": ["name", "value"]
      }
    },
    {
      "name": "discover_cvars",
      "description": "Enumerate all Console Variables (CVars) whose names begin with a given prefix. Use this to discover available optimization knobs for a specific system. For example, prefix 'r.Lumen' lists all Lumen quality/performance CVars. Returns name, current value, and help text.",
      "input_schema": {
        "type": "object",
        "properties": {
          "prefix": {
            "type": "string",
            "description": "CVar name prefix to search. Examples: 'r.', 'r.Lumen', 'r.Shadow', 'r.Bloom', 'a.', 'p.', 'foliage.'"
          },
          "max_results": {
            "type": "integer",
            "description": "Maximum number of CVars to return (default: 50)"
          }
        },
        "required": ["prefix"]
      }
    },
    {
      "name": "execute_console_command",
      "description": "Execute any GEngine console command that is not a CVar set/get. Use for:\n- Profiling: 'Trace.Start cpu,gpu,memory', 'Trace.Stop', 'ProfileGPU', 'memreport -full'\n- A/B testing: 'abtest r.BloomQuality 0 5' (auto-toggles between values and measures delta)\n- Stat commands: 'stat fps', 'stat unit'\n- Freeze/unfreeze: 'FreezeRendering' (halts GPU for occlusion culling analysis)\n- Garbage collection: 'obj gc'\nNOTE: Commands that freeze the viewport (FreezeRendering, ProfileGPU) must be manually unfreezed by the user. Use with caution.",
      "input_schema": {
        "type": "object",
        "properties": {
          "command": {
            "type": "string",
            "description": "Full console command string to execute, e.g. 'Trace.Start cpu,gpu', 'abtest r.BloomQuality 0 5', 'memreport -full'"
          }
        },
        "required": ["command"]
      }
    },
    {
      "name": "start_csv_profiler",
      "description": "Start the Unreal Engine CSV Profiler to record frame time statistics to a .csv file in Saved/Profiling/. The CSV captures stat unit timings (Frame, Game, Draw, GPU) over time, enabling post-hoc analysis of mean frame times, standard deviations, and stutter detection. Stop with stop_csv_profiler, then read with read_profiling_file. Best practice: run a repeatable camera flythrough while profiling for consistent data.",
      "input_schema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "stop_csv_profiler",
      "description": "Stop the CSV Profiler and list all CSV files in Saved/Profiling/. Returns the paths of available profiling output files so you can pass them to read_profiling_file for analysis.",
      "input_schema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "read_profiling_file",
      "description": "Read the contents of a profiling output file from Saved/Profiling/ (CSV or .log). If no file_name is given, lists all available files so you can choose. Automatically truncates large files to max_lines (default: 200). Use this to:\n- Parse CSV profiler output: look for high FrameTime, GameThreadTime, RenderThreadTime, GPUTime columns\n- Read memreport logs: look for 'Listing all textures' (sorted by size) and 'Pooled Render Targets'\n- Read trace logs from Unreal Insights sessions",
      "input_schema": {
        "type": "object",
        "properties": {
          "file_name": {
            "type": "string",
            "description": "Filename (relative to Saved/Profiling/) or absolute path. Omit to list all available files."
          },
          "max_lines": {
            "type": "integer",
            "description": "Maximum lines to return from the file (default: 200). Increase for large CSV files."
          }
        },
        "required": []
      }
    },
    {
      "name": "get_scalability_settings",
      "description": "Read the current Unreal Engine quality/scalability level settings: resolution quality (%), view distance, anti-aliasing, shadow, global illumination, reflection, post-process, texture, effects, foliage, and shading quality. All discrete levels use 0=Low, 1=Medium, 2=High, 3=Epic scale. Use this before set_scalability_settings to establish a baseline.",
      "input_schema": {
        "type": "object",
        "properties": {},
        "required": []
      }
    },
    {
      "name": "set_scalability_settings",
      "description": "Apply Unreal Engine scalability quality level overrides. All integer quality levels: 0=Low, 1=Medium, 2=High, 3=Epic. Resolution quality is a float percentage (25.0-100.0). Set save_to_ini=true to persist settings to GameUserSettings.ini via UGameUserSettings — otherwise changes are transient. Typical optimization profiles:\n- Mobile target: resolution_quality=75, shadow_quality=1, effects_quality=1, texture_quality=2\n- High-end PC: resolution_quality=100, all qualities=3\n- Mid-range: resolution_quality=100, shadow_quality=2, global_illumination_quality=2, effects_quality=2",
      "input_schema": {
        "type": "object",
        "properties": {
          "resolution_quality": {
            "type": "number",
            "description": "Internal render resolution as percentage (25.0 to 100.0). Values below 100 trigger upscaling (TSR/TAA/FSR)."
          },
          "view_distance_quality": {
            "type": "integer",
            "description": "View/draw distance quality: 0=Low, 1=Medium, 2=High, 3=Epic"
          },
          "anti_aliasing_quality": {
            "type": "integer",
            "description": "Anti-aliasing quality: 0=Low (FXAA), 1=Medium, 2=High, 3=Epic (TSR)"
          },
          "shadow_quality": {
            "type": "integer",
            "description": "Shadow quality: 0=Low (no dynamic shadows), 1=Medium, 2=High, 3=Epic"
          },
          "global_illumination_quality": {
            "type": "integer",
            "description": "Global illumination quality (Lumen): 0=Low, 1=Medium, 2=High, 3=Epic"
          },
          "reflection_quality": {
            "type": "integer",
            "description": "Reflection quality (Lumen/SSR): 0=Low, 1=Medium, 2=High, 3=Epic"
          },
          "post_process_quality": {
            "type": "integer",
            "description": "Post-processing quality (bloom, DOF, lens flare): 0=Low, 1=Medium, 2=High, 3=Epic"
          },
          "texture_quality": {
            "type": "integer",
            "description": "Texture streaming quality and LOD bias: 0=Low (force lower mips), 1=Medium, 2=High, 3=Epic (full res)"
          },
          "effects_quality": {
            "type": "integer",
            "description": "Particle/VFX quality: 0=Low, 1=Medium, 2=High, 3=Epic"
          },
          "foliage_quality": {
            "type": "integer",
            "description": "Foliage/grass density quality: 0=Low, 1=Medium, 2=High, 3=Epic"
          },
          "shading_quality": {
            "type": "integer",
            "description": "Shading model quality: 0=Low, 1=Medium, 2=High, 3=Epic"
          },
          "save_to_ini": {
            "type": "boolean",
            "description": "If true, persist settings to GameUserSettings.ini via UGameUserSettings (default: false = transient)"
          }
        },
        "required": []
      }
    },
    {
      "name": "get_renderer_settings",
      "description": "Read properties of URendererSettings — the C++ equivalent of the Project Settings > Rendering section in the editor. These settings are persisted to DefaultEngine.ini under [/Script/Engine.RendererSettings]. Use filter to narrow output to a specific system (e.g., filter='Lumen' for Lumen settings, filter='VirtualTexture' for VT settings).\n\nKey properties to know:\n- bDefaultFeatureAutoExposure — enable/disable auto exposure\n- DefaultFeatureAntiAliasing — anti-aliasing method (0-4: none/FXAA/TAA/MSAA/TSR)\n- bEnableVirtualTexturing — enable virtual texture support\n- bSupportHardwareRayTracing — enable hardware ray tracing\n- GlobalIlluminationMode — GI method (None/SSGI/Lumen/ScreenSpaceGI)\n- ReflectionCaptureResolution — reflection capture cubemap resolution\n- NaniteMeshes — whether Nanite is active\n- bUseGPUMorphTargets — GPU morph target evaluation",
      "input_schema": {
        "type": "object",
        "properties": {
          "filter": {
            "type": "string",
            "description": "Optional substring filter on property names, e.g. 'Lumen', 'VirtualTexture', 'Shadow', 'AntiAliasing'"
          }
        },
        "required": []
      }
    },
    {
      "name": "set_renderer_setting",
      "description": "Persistently set a URendererSettings property via the reflection system. Uses GetMutableDefault<URendererSettings>() → PostEditChangeProperty() → SaveConfig() — identical to manually editing the Project Settings > Rendering UI. Change is saved to DefaultEngine.ini and the editor UI updates immediately. Some settings require an editor restart to fully apply.\n\nCommon optimization targets:\n| Property | Values | Purpose |\n|----------|--------|----------|\n| DefaultFeatureAntiAliasing | 0/1/2/3/4 | none/FXAA/TAA/MSAA/TSR |\n| bDefaultFeatureAutoExposure | True/False | disable for manual EV100 control |\n| bEnableVirtualTexturing | True/False | VT reduces peak texture memory |\n| bSupportHardwareRayTracing | True/False | disable on non-RTX targets |\n| GlobalIlluminationMode | 0/1/2 | None/SSGI/Lumen |\n| ReflectionCaptureResolution | 64/128/256/512 | cubemap res (power of 2) |\n| bUseGPUMorphTargets | True/False | GPU-side morph target evaluation |",
      "input_schema": {
        "type": "object",
        "properties": {
          "property": {
            "type": "string",
            "description": "Exact property name on URendererSettings, e.g. 'bEnableVirtualTexturing'. Use get_renderer_settings to list all available properties."
          },
          "value": {
            "type": "string",
            "description": "New value as string. Booleans: 'True'/'False'. Integers: '0', '1', etc. Enums: integer index."
          }
        },
        "required": ["property", "value"]
      }
    }
  ]
}
